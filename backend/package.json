{
  "name": "apnakhata-backend",
  "version": "1.0.0",
  "private": true,
  "description": "ApnaKhata API gateway — HTTP layer over the ledger, inventory, and credit services",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "tsc && node dist/server.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.0",
    "@types/pg": "^8.11.6",
    "typescript": "^5.5.3"
  },
  "engines": {
    "node": ">=18"
  }
}
